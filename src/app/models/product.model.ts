export interface Product {
  productId: number;
  productName: string;
  description: string;
  price: number;
  contactNumber: string;
  image: string;
  category: string;
  publishedBy: string;
  email: string;
  published: boolean;
}
